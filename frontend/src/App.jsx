import React, { useState, useEffect } from "react";import axios from "axios";const API_URL = "http://localhost:5000/api/employees";export default function App() {const [employees, setEmployees] = useState([]);const [editingId, setEditingId] = useState(null);const [formData, setFormData] = useState({ employeeId: "", name: "", department: "", designation: "", salary: "", email: "" });useEffect(() => { fetchEmployees(); }, []);const fetchEmployees = async () => { try { const res = await axios.get(API_URL); setEmployees(res.data); } catch (err) { console.error("API error", err); } };const handleInput = (e) => { setFormData({ ...formData, [e.target.name]: e.target.value }); };const handleSubmit = async (e) => { e.preventDefault(); try { if (editingId) { await axios.put(`${API_URL}/${editingId}`, formData); setEditingId(null); } else { await axios.post(API_URL, formData); } setFormData({ employeeId: "", name: "", department: "", designation: "", salary: "", email: "" }); fetchEmployees(); } catch (err) { alert(err.response?.data?.message || "Operation failed"); } };const triggerEdit = (emp) => { setEditingId(emp.employeeId); setFormData(emp); };const triggerDelete = async (id) => { if (window.confirm("Delete record?")) { try { await axios.delete(`${API_URL}/${id}`); fetchEmployees(); } catch (err) { alert("Delete failed"); } } };return (<div style={{ padding: "30px", fontFamily: "system-ui, sans-serif", maxWidth: "1000px", margin: "0 auto" }}><h2>Employee Management System</h2><form onSubmit={handleSubmit} style={{ background: "#f8fafc", padding: "20px", borderRadius: "8px", border: "1px solid #e2e8f0", margin: "20px 0", display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(150px, 1fr))", gap: "10px" }}><input name="employeeId" placeholder="Employee ID" value={formData.employeeId} onChange={handleInput} disabled={!!editingId} required style={{ padding: "8px" }} /><input name="name" placeholder="Name" value={formData.name} onChange={handleInput} required style={{ padding: "8px" }} /><input name="department" placeholder="Department" value={formData.department} onChange={handleInput} required style={{ padding: "8px" }} /><input name="designation" placeholder="Designation" value={formData.designation} onChange={handleInput} required style={{ padding: "8px" }} /><input name="salary" type="number" placeholder="Salary" value={formData.salary} onChange={handleInput} required style={{ padding: "8px" }} /><input name="email" type="email" placeholder="Email" value={formData.email} onChange={handleInput} required style={{ padding: "8px" }} /><div style={{ gridColumn: "1 / -1", display: "flex", justifyContent: "flex-end", gap: "10px" }}>{editingId && <button type="button" onClick={() => { setEditingId(null); setFormData({ employeeId: "", name: "", department: "", designation: "", salary: "", email: "" }); }}>Cancel</button>}<button type="submit" style={{ background: "#2563eb", color: "white", padding: "8px 16px", border: "none", borderRadius: "4px", cursor: "pointer" }}>{editingId ? "Update" : "Add Employee"}</button></div></form><table style={{ width: "100%", borderCollapse: "collapse", marginTop: "20px" }}><thead style={{ background: "#1e293b", color: "white" }}><tr><th style={{ padding: "10px", textAlign: "left" }}>ID</th><th style={{ padding: "10px", textAlign: "left" }}>Name</th><th style={{ padding: "10px", textAlign: "left" }}>Department</th><th style={{ padding: "10px", textAlign: "left" }}>Designation</th><th style={{ padding: "10px", textAlign: "left" }}>Salary</th><th style={{ padding: "10px", textAlign: "left" }}>Email</th><th style={{ padding: "10px", textAlign: "left" }}>Actions</th></tr></thead><tbody>{employees.map(emp => (<tr key={emp.employeeId} style={{ borderBottom: "1px solid #e2e8f0" }}><td style={{ padding: "10px" }}>{emp.employeeId}</td><td style={{ padding: "10px" }}>{emp.name}</td><td style={{ padding: "10px" }}>{emp.department}</td><td style={{ padding: "10px" }}>{emp.designation}</td><td style={{ padding: "10px" }}>INR {emp.salary}</td><td style={{ padding: "10px" }}>{emp.email}</td><td style={{ padding: "10px" }}><button onClick={() => triggerEdit(emp)} style={{ marginRight: "5px" }}>Edit</button><button onClick={() => triggerDelete(emp.employeeId)} style={{ background: "#ef4444", color: "white", border: "none", padding: "4px 8px", borderRadius: "3px", cursor: "pointer" }}>Delete</button></td></tr>))}{employees.length === 0 && <tr><td colSpan="7" style={{ textAlign: "center", padding: "20px" }}>No employee records found.</td></tr>}</tbody></table></div>);}
